# the execution agent that interacts/tool calls to droidrun to fetch info from device or execute tasks
# if execution fails, infers the reason for failure and loops back to the retrieval agent via orchestration layer
